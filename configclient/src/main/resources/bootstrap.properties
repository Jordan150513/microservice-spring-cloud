spring.cloud.config.uri=http://localhost:50901
spring.application.name=configclient

spring.cloud.config.name=configclient
spring.cloud.config.label=master
spring.cloud.config.profile=dev  
 # 在这里控制了 目前环境 dev/ test/ prod 不能在application.properties中指定环境了，否则不会去请求仓库里的数据。
#searchpath 只能是指定该configserver 的映射的配置文件所在仓库的

management.security.enabled=false
#设置成不需要安全认证，就可以直接在终端用curl语句去操作post请求刷新client的配置文件C:\Users\qiaodan>curl -X POST http://localhost:50902/refresh
#["name"]